package JavaCompleto.TratamentoDeExecao;

public class TiposDeErros {
    //Erros de compilação não faz .java -> .class
    //Erros de execução acontece no tempo que esta executando

    //hierarquia de erro
    //1º -Throwable <- lança erro antes de tudo da pra lançar todos erros dele
    //2º -Error(Muito grave sem solução)/Exception(Erros tratáveis) <- ambos herdam de Throwable

    //Ler a pilha de métodos 
    //Ler os ERROS!!!  com calma e paciência 
}
