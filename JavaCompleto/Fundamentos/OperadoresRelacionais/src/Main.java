public class Main {
    public static void main(String[] args) {
        double media =7.6;
        String resultado = media >= 7.0 ? "Aprovado" : "Recusado";  // com o ? vc atribui dois valores
                                                                    // no caso o 1° antes se verdadeiro: e o segundo
                                                                    // depois se falso.
        System.out.println("o aluno esta"+ resultado);
    }
}